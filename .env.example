# Supabase — Settings > API no painel do projeto
# Usadas apenas no backend (server actions). Nunca expostas ao navegador.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI — usado no pipeline de IA (extração de lançamentos e insights)
OPENAI_API_KEY=
